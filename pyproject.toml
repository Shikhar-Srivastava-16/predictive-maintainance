[project]
name = "predictive-maintainance"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.12.1",
    "ucimlrepo>=0.0.7",
    "scikit-learn>=1.6.0",
]
